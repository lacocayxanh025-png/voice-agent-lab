# GitHub Repository Setup

Recommended repository name:

```text
voice-agent-lab
```

Recommended description:

```text
Privacy-first toolkit for testing voice-agent intent routing, escalation rules, and improvement queues with synthetic conversations.
```

Recommended topics:

```text
voice-agent
intent-routing
conversation-design
synthetic-data
contact-center
codex
privacy
```

Recommended first issues:

1. Add config schema validation.
2. Add more multilingual synthetic fixtures.
3. Build a static reviewer console from the CLI engine.
4. Add an improvement queue export command.
5. Add optional local-only ASR/TTS adapter interfaces.

Recommended first pull request title:

```text
Initial privacy-first voice agent routing lab
```
